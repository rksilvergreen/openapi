# Test: Property with conflicting const values (should fail validation)
openapi: 3.0.0
info:
  title: Invalid Property Const Conflict Test
  version: 1.0.0
paths: {}
components:
  schemas:
    Test1:
      type: string
      allOf:
        - $ref: "#/components/schemas/Test2"
    Test2:
      type: string
      allOf:
        - $ref: "#/components/schemas/Test1"
